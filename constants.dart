 import 'package:flutter/material.dart';
const labelTextStyle=Textstyle(fontsize:80,colour:colours.red);
const kbottomContainerHeight = 80.0;
 const kactiveCardColour=Color();
 const kinactiveCardColour=Color();